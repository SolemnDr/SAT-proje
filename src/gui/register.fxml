<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox alignment="CENTER" prefHeight="560" prefWidth="960" spacing="0" styleClass="root-pane" stylesheets="@/style/dark.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="gui.RegisterController">

    <VBox alignment="CENTER" spacing="6" style="-fx-padding: 0 0 24 0;">
        <Label styleClass="logo-title" text="GameStore">
         <VBox.margin>
            <Insets top="40.0" />
         </VBox.margin></Label>
        <Label styleClass="logo-sub" text="Yeni Hesap Oluştur" />
      <VBox.margin>
         <Insets />
      </VBox.margin>
    </VBox>

    <VBox alignment="CENTER" spacing="14" style="-fx-padding: 0 36 0 36;">

        <VBox spacing="5">
            <Label styleClass="field-label" text="KULLANICI ADI" />
            <TextField fx:id="usernameField" promptText="Kullanıcı adınızı giriniz" styleClass="field-input" />
        </VBox>

        <VBox spacing="5">
            <Label styleClass="field-label" text="E-POSTA" />
            <TextField fx:id="emailField" promptText="e-posta adresinizi giriniz" styleClass="field-input" />
        </VBox>

        <VBox spacing="5">
            <Label styleClass="field-label" text="ŞİFRE" />
            <PasswordField fx:id="passwordField" promptText="Şifrenizi giriniz" styleClass="field-input" />
        </VBox>

        <VBox spacing="5">
            <Label styleClass="field-label" text="HESAP TÜRÜ" />
            <ComboBox fx:id="roleComboBox" maxWidth="Infinity" style="-fx-background-color: #c0c0f0;" styleClass="field-input" />
        </VBox>

        <Button maxWidth="Infinity" onAction="#handleRegister" styleClass="btn-primary" text="Hesap Oluştur">
         <VBox.margin>
            <Insets top="-22.0" />
         </VBox.margin></Button>

        <Label alignment="CENTER" maxWidth="Infinity" styleClass="divider-label" text="Zaten hesabın var mı?" />

        <Button maxWidth="Infinity" onAction="#handleLogin" styleClass="btn-primary" text="Giriş Yap" />

        <Label fx:id="errorLabel" styleClass="error-label" text="" wrapText="true">
         <VBox.margin>
            <Insets bottom="-20.0" top="20.0" />
         </VBox.margin></Label>
      <VBox.margin>
         <Insets />
      </VBox.margin>

    </VBox>
   <padding>
      <Insets bottom="50.0" />
   </padding>
</VBox>
